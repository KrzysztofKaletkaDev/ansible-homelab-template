services:
  blocky:
    image: spx01/blocky:v0.31.0
    container_name: blocky
    restart: unless-stopped
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    volumes:
      - ./config.yml:/app/config.yml
    environment:
      - TZ={{ timezone | default('Europe/Warsaw') }}
